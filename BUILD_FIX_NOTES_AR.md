# ملاحظات إصلاح البناء

تم إصلاح أخطاء البناء التي ظهرت بعد إضافة مكتبة `sharp` وتشغيل `npm run build`.

## الإصلاحات المنفذة

1. إصلاح ملف `src/lang/seo-money-pages.ts`
   - تمت إضافة تعريف المتغير المحلي `u` داخل دالة `makePage`.
   - يعتمد `u` على لغة الصفحة الحالية مع fallback آمن إلى العربية:
     `const u = ui[lang] ?? ui.ar`
   - تم توحيد استخدام نص المقارنة/الحل من نفس المتغير المحلي بدل الرجوع المباشر إلى `ui[lang]`.

2. إصلاح ملف `src/lib/seo.ts`
   - تمت إزالة الاستيراد غير المستخدم `getHomeFaqItems` لأنه كان يسبب خطأ TypeScript من نوع `TS6133`.

3. ملاحظة الاعتمادات
   - ملفا `package.json` و `package-lock.json` يحتويان بالفعل على `sharp` ضمن `devDependencies`، لذلك عند تشغيل `npm install` ثم `npm run build` على جهاز التطوير سيتم توفر مكتبة تحسين الصور.

## أمر الفحص المطلوب بعد فك الضغط

```powershell
npm install
npm run build
```

## ملاحظة بيئة الفحص

داخل بيئة المعالجة الحالية لا يوجد اتصال متاح بسجل npm، لذلك لا يمكن إعادة تنزيل `node_modules` بالكامل من registry.npmjs.org. تم إصلاح أخطاء المصدر الظاهرة من سجل البناء، والتحقق يدويًا من مواضع الأخطاء التي كانت تمنع TypeScript.
