# ملاحظات استيراد المشروع إلى v0

هذه نسخة السورس كود الأخيرة بعد تعديلات الصور والواجهة، بصيغة مناسبة للاستيراد داخل منصة v0.

## طريقة الاستخدام
1. ارفع ملف ZIP إلى منصة v0.
2. تأكد أن v0 يتعرف على `package.json` من جذر المشروع.
3. بعد الاستيراد يمكن تشغيل المشروع بالأوامر المعتادة:

```bash
npm install
npm run dev
```

## ملاحظات مهمة
- لم يتم تضمين `node_modules`.
- لم يتم تضمين `dist` حتى تكون النسخة خفيفة ونظيفة للاستيراد.
- الصور موجودة داخل `public/assets/images`.
- `favicon.ico` موجود داخل `public`.
- المشروع يحافظ على RTL والعربية كلغة أساسية.
